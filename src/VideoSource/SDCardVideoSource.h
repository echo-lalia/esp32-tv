
// #pragma once

// #include "ChannelData/SDCardChannelData.h"
// // #include "VideoSource.h"

// class SDCardVideoSource : public VideoSource
// {
// private:
//   ChannelData *mChannelData;
//   int mFrameCount = 0;

// public:
//   SDCardVideoSource(ChannelData *channelData);
//   void start();
//   // see superclass for documentation
//   bool getVideoFrame(uint8_t **buffer, size_t &bufferLength, size_t &frameLength);
//   void setChannel(int channel) {
//     mFrameCount = 0;
//   }
// };
